version: "3.9"

services:
  radisson:
    build: .
    container_name: radisson
    ports:
      - "8501:8501"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
